
# Ядро и заголовки, прошивки
depends+=(linux-firmware)

# Пакеты для файловых систем
depends+=(btrfs-progs cifs-utils dosfstools e2fsprogs ntfs-3g squashfs-tools)

# LVM и RAID
depends+=(lvm2 mdadm)

# Базовые консольные утилиты и оболочки
depends+=(atop bash busybox chntpw cpio curl debootstrap duperemove efibootmgr htop iftop inotify-tools iotop iperf jq mc mtr nano powertop pv rsync screen sqlite3 sudo xxd)

# Системные службы
depends+=(irqbalance openssh-server openvpn)

# Зависимости для Python
depends+=(python3-pip)
