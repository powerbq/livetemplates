version: '3.6'

services:
  livearch-system:
    build:
      args:
        - MIRROR_COUNTRY_CODE=UA
    environment:
      - AUR_PACKAGES=c-client php-custom php-custom-extras composer-custom symfony-cli-bin nvm google-chrome oh-my-zsh-git slack-desktop teams-for-linux-bin teamviewer zoom moonlight-qt-bin sunshine-bin

  livearch-boot:
    build:
      args:
        - MIRROR_COUNTRY_CODE=UA
